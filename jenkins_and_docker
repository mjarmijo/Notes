Jenkins and Docker commands

$ docker push mjamijo/jenkins:tagname

# start the jenkins server
java -jar jenkins.war --httpPort=8080
# browse to http://localhost:8080


# List images
docker images -a

#list containers
docker ps -a
